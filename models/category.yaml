schema:
    name: string
    description: text
    order: 
        type: integer
        default: 0
    remark: text
    parent_id:
        type: integer
        default: 0
    model: string|32
    model_id:
        type: integer
        default: 0
default:
    columns:
    - name
    